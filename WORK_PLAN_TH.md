# ตารางแผนงานโปรเจกต์ Security-Alert

อ้างอิงจากรายงาน `PROJECT_REVIEW_TH.md` (ตรวจเมื่อ 18 กรกฎาคม 2026)  
แผนเริ่มต้น: 20 กรกฎาคม 2026  
ระยะเวลาโดยประมาณ: 6 สัปดาห์

## เป้าหมาย

พัฒนา Security-Alert จาก MVP ช่วงต้นให้เป็นระบบสาธิตที่รับ Security Alert แล้วแนะนำ MITRE ATT&CK Technique จากข้อมูลเวอร์ชันที่กำหนด พร้อมหลักฐานอ้างอิง การส่งต่อให้มนุษย์ตรวจ และชุดทดสอบที่ทำซ้ำได้

## ตารางการทำงาน

| ช่วงเวลา | ลำดับ | งาน | ผลลัพธ์ที่ต้องส่งมอบ | งานที่ต้องเสร็จก่อน | สถานะ |
|---|---:|---|---|---|---|
| สัปดาห์ 1<br>20–26 ก.ค. 2026 | P0 | ยกเลิก/หมุน secret ที่เคยอยู่ใน Git และหยุด track `.env` | ไม่มี secret ในไฟล์ที่ track, มี `.env.example` ที่ไม่มีค่าจริง | – | เสร็จแล้ว |
| สัปดาห์ 1 | P0 | เพิ่ม `.gitignore` และนำ `__pycache__`/`.pyc` ออกจาก tracking | repository ไม่มีไฟล์ generated bytecode | – | เสร็จแล้ว |
| สัปดาห์ 1 | P0 | แก้ output path ของ STIX ingestion | regenerate ข้อมูลลง `data/processed/` ได้ถูกตำแหน่ง | – | เสร็จแล้ว |
| สัปดาห์ 1 | P0 | เพิ่ม README และ pin/lock dependencies | setup และ start API ใน clean environment ได้ตาม README | – | เสร็จแล้ว |
| สัปดาห์ 1 | P0 | เขียน test ขั้นต้นสำหรับ schema, ingestion และ taxonomy API | pytest รัน test จริงและผ่าน | งาน repository hygiene | เสร็จแล้ว |
| สัปดาห์ 2<br>27 ก.ค.–2 ส.ค. 2026 | P1 | เลือก embedding backend และรูปแบบ index | มี decision note ระบุเหตุผล ข้อจำกัด และวิธี reproduce | clean setup | ยังไม่เริ่ม |
| สัปดาห์ 2 | P1 | ทำ `embedder.py` และสร้าง index จาก technique candidates | สร้าง index พร้อม metadata tactic/platform/source ได้ | embedding decision | ยังไม่เริ่ม |
| สัปดาห์ 2 | P1 | ปรับ schema ให้รองรับ technique หลาย tactics | ไม่สูญเสียความสัมพันธ์ tactic จาก STIX | ingestion fix | ยังไม่เริ่ม |
| สัปดาห์ 2 | P1 | ทำ `retriever.py` พร้อม tactic filter และ top-k | ค้น candidate จาก alert ได้โดยไม่คืน ID นอก pinned dataset | index, schema | ยังไม่เริ่ม |
| สัปดาห์ 2 | P1 | เพิ่ม retrieval tests และชุดวัด top-k recall ขั้นต้น | มีผล baseline ที่ทำซ้ำได้ | retriever | ยังไม่เริ่ม |
| สัปดาห์ 3<br>3–9 ส.ค. 2026 | P1 | ทำ `technique_inferencer.py` ด้วย structured output | เลือกและให้คะแนน technique จาก retrieved candidates เท่านั้น | retriever | ยังไม่เริ่ม |
| สัปดาห์ 3 | P1 | ทำ `evidence_linker.py` | prediction ทุกตัวมี evidence ที่ย้อนกลับไปยัง input ได้ | inferencer | ยังไม่เริ่ม |
| สัปดาห์ 3 | P1 | ทำ `grounding_judge.py` | ตรวจ ID, tactic, evidence และคำอธิบายกับ MITRE/input | inferencer, evidence linker | ยังไม่เริ่ม |
| สัปดาห์ 3 | P1 | กำหนด confidence และ `needs_human_review` | มี threshold สำหรับ low-confidence, ambiguous และ no-match | grounding judge | ยังไม่เริ่ม |
| สัปดาห์ 3 | P1 | ทดสอบ prompt injection และ malformed model output | input ที่ผิดปกติไม่ทำให้ pipeline คืนผลที่ไม่ grounded | pipeline agents | ยังไม่เริ่ม |
| สัปดาห์ 4<br>10–16 ส.ค. 2026 | P1 | ต่อ pipeline จริงเข้ากับ `POST /alerts/infer` | endpoint ไม่มี T1110/T1059.001 ที่ hard-code | งานสัปดาห์ 3 | ยังไม่เริ่ม |
| สัปดาห์ 4 | P1 | เพิ่ม validation, timeout, retry และแยกประเภท error | client ไม่เห็น exception ภายใน และ provider failure ถูกจัดการ | pipeline integration | ยังไม่เริ่ม |
| สัปดาห์ 4 | P1 | โหลด prompt จาก `prompts/v1/` และบันทึกเวอร์ชัน | เปลี่ยน prompt ได้โดยไม่แก้ source agent | pipeline integration | ยังไม่เริ่ม |
| สัปดาห์ 4 | P1 | เพิ่ม unit/integration/API tests โดย mock Gemini | test deterministic และไม่ใช้ API quota | pipeline integration | ยังไม่เริ่ม |
| สัปดาห์ 4 | P2 | เพิ่ม structured logging และ request ID | ติดตาม request และสาเหตุ error ได้ | API error handling | ยังไม่เริ่ม |
| สัปดาห์ 5<br>17–23 ส.ค. 2026 | P1 | สร้าง labeled evaluation dataset | ครอบคลุม positive, multi-technique, ambiguous และ no-match | pipeline จริง | ยังไม่เริ่ม |
| สัปดาห์ 5 | P1 | ทำ `eval/metrics.py` และ `eval/run_eval.py` | รายงาน precision/recall/F1, top-k recall, tactic accuracy และ review rate | evaluation dataset | ยังไม่เริ่ม |
| สัปดาห์ 5 | P1 | บันทึก model/prompt/dataset/STIX version ในผล eval | เปรียบเทียบผลแต่ละรุ่นได้อย่างทำซ้ำได้ | evaluation runner | ยังไม่เริ่ม |
| สัปดาห์ 5 | P1 | เพิ่ม CI สำหรับ test และ evaluation smoke test | ทุก pull request ตรวจ regression อัตโนมัติ | automated tests | ยังไม่เริ่ม |
| สัปดาห์ 5 | P2 | ตกลงเกณฑ์คุณภาพขั้นต่ำของ MVP | มีเกณฑ์ผ่าน/ไม่ผ่านที่วัดได้ | evaluation baseline | ยังไม่เริ่ม |
| สัปดาห์ 6<br>24–30 ส.ค. 2026 | P2 | ทำ UI สำหรับ alert, prediction, confidence, evidence และ analyst review | ผู้ใช้ทดลอง workflow ตั้งแต่กรอก alert ถึง review ได้ | API/evaluation พร้อม | ยังไม่เริ่ม |
| สัปดาห์ 6 | P1 | จำกัด CORS และเพิ่ม auth/rate limit ตามรูปแบบ deploy | API ไม่เปิดกว้างโดยไม่จำเป็น | deployment decision | ยังไม่เริ่ม |
| สัปดาห์ 6 | P1 | กำหนด privacy/retention policy สำหรับ alert | ระบุการจัดเก็บ IP, hostname, account และข้อมูลอ่อนไหว | – | ยังไม่เริ่ม |
| สัปดาห์ 6 | P2 | เพิ่ม MITRE attribution, advisory disclaimer และ LICENSE | ข้อจำกัดและแหล่งข้อมูลแสดงชัดเจน | UI/README | ยังไม่เริ่ม |
| สัปดาห์ 6 | P2 | package/deploy และทำ acceptance/security test | มี release candidate ที่ติดตั้งและตรวจรับได้ | งานทั้งหมดข้างต้น | ยังไม่เริ่ม |

## จุดตรวจรับรายสัปดาห์

| จุดตรวจ | เกณฑ์ผ่าน |
|---|---|
| สิ้นสัปดาห์ 1 | ผ่านตามการตรวจรับของเจ้าของโปรเจกต์: clean setup พร้อมใช้งาน, ไม่มี `.env` ถูก track, มี README/setup docs, ingestion fix และ test ขั้นต้น |
| สิ้นสัปดาห์ 2 | retriever คืน top-k จาก pinned ATT&CK subset พร้อมผล recall baseline |
| สิ้นสัปดาห์ 3 | inference ทุกผลมี evidence/grounding และส่งเคสไม่แน่ใจให้มนุษย์ตรวจ |
| สิ้นสัปดาห์ 4 | `/alerts/infer` ใช้ pipeline จริง และ test ไม่เรียก Gemini live |
| สิ้นสัปดาห์ 5 | CI ผ่านและมี evaluation report ที่ระบุเวอร์ชันครบ |
| สิ้นสัปดาห์ 6 | UI ใช้งาน end-to-end ได้และ release candidate ผ่าน acceptance/security test |

## Definition of Done สำหรับ MVP

- ติดตั้งและ start server จาก README ได้ใน clean environment
- `/alerts/infer` ไม่มีผล mock/hard-code และไม่คืน technique นอก pinned ATT&CK subset
- prediction ทุกตัวมี evidence ที่ตรวจย้อนกลับไปยัง input ได้
- invalid, ambiguous และ no-match input ถูกส่งเข้า human review ตามเกณฑ์
- unit, integration และ API tests รันใน CI โดยไม่เรียก Gemini จริง
- มี evaluation report และผ่านเกณฑ์ขั้นต่ำที่ทีมกำหนด
- ไม่มี secret ใน repository และ production config ไม่เปิด CORS แบบ wildcard
- แสดง MITRE attribution, advisory disclaimer และข้อจำกัดของระบบอย่างชัดเจน

## วิธีอัปเดตตาราง

เปลี่ยนคอลัมน์ `สถานะ` เป็น `กำลังทำ`, `ติดขัด` หรือ `เสร็จแล้ว` และบันทึกสาเหตุ/ลิงก์ Pull Request ไว้ในข้อความ commit หรือเพิ่มคอลัมน์ `หมายเหตุ` เมื่อเริ่มแบ่งงานให้สมาชิกทีม
